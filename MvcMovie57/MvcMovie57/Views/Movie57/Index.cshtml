@model IEnumerable<MvcMovie57.Models.Movie57>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title57)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReleaseDate57)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre57)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price57)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title57)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate57)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre57)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price57)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
